# Thesis Citation Management Workflow

## Golden Rule

**Never add references to `.bib` directly. All references come from Zotero.** The workflow is:

1. **User finds a source** (via web search, Gemini, etc.)
2. **User adds it to Zotero** with a CamelCase citation key (e.g., `gomezParetoPID2020`)
3. **User syncs Zotero** → Zotero's Better BibTeX exports to `references.bib`
4. **I check the `.bib`** for the new entry and its exact key name
5. **I fix the `.tex`** to use `\cite{EXACT_KEY_FROM_BIB}`
6. **The citation key is now in the `.bib`** — I use the exact key name in `.tex`

## What I Should NEVER Do

- ❌ Add entries to `references.bib` myself
- ❌ Rename keys in `references.bib` (Zotero owns them)
- ❌ Web-search for sources unless explicitly asked (user prefers Gemini for that)
- ❌ Use `thebibliography` blocks — all entries must be in `references.bib`

## Citation Key Convention

Zotero should use CamelCase keys: `gomezParetoPID2020`, `mahmudDevelopmentCollisionResilient2021`
Zotero's auto-generated keys may use snake_case — if so, note the mismatch and the user can fix in Zotero.

## File Structure

- `main.tex` — routes to `references.bib` via `\bibliography{IEEEabrv,references}`
- `Sections/*.tex` — contain `\cite{key}` commands
- `references.bib` — auto-generated by Zotero Better BibTeX export (read-only for me)
- `citations_checklist.md` — living document tracking all citation issues
- `thesis_skill.md` — this file (workflow documentation)

## Priority Order When Fixing Citations

1. **Category A**: Keys already in `.bib` with wrong `.tex` name → fix `.tex` key to match `.bib`
2. **Category B**: Keys missing from `.bib` → tell user which need Zotero entries
3. **Category C**: Template/placeholder files → keep or delete per user preference
4. **Category D**: Plain-text claims needing citations → provide context, user searches

## Common Pitfalls

- Zotero sync doesn't happen automatically — user must sync before `.bib` updates
- The `.bib` key names are CASE-SENSITIVE — `gomboc` ≠ `Gomboc2026`
- Docker container citations (`docker:install`, `docker:postinstall`) get auto-generated Zotero keys with timestamps
- The Italian/EU thrust test site entry is T-Motor, not EMAX — used for "comparable motor" claim
- Oscar Liang URLs are two separate entries in Zotero (motors guide + LiPo guide)

## Placeholder Markers

For sections/content that are incomplete but should remain visible in the PDF (not LaTeX comments):

- `***NEED TO ADD A SOURCE***` — when a citation/source is still missing
- `***HERE NEEDS TO BE SECTION ON <TOPIC>***` — when a section stub needs content

These render as bold monospace in the PDF, so both humans and LLMs can spot them on a regen pass.
